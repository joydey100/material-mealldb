import { makeStyles } from "@mui/styles";

const useStyles = makeStyles({
  links: {
    color: "#fff",
    textDecoration: "none",
    fontSize: "20px",
    marginRight: "10px",
  },
  btn: {
    textDecoration: "none",
    padding: "10px 30px",
    background: "#1976D2",
    color: "#fff",
    borderRadius: "15px",
  },
});

export default useStyles;
